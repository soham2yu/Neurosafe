## Packages
framer-motion | Animation library for premium feel and page transitions
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility to merge Tailwind classes efficiently

## Notes
External API located at import.meta.env.VITE_API_BASE_URL (https://neurosafe-backend.onrender.com)
Authentication is simulated via localStorage ("neurosafe_user")
History is stored in localStorage ("neurosafe_history")
Tailwind Config needs to support 'font-display' and 'font-body' as per design system
